SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/moliyaviy?stringtype=unspecified
SPRING_DATASOURCE_USERNAME=moliyaviy
SPRING_DATASOURCE_PASSWORD=moliyaviy

# HMAC key for signing JWTs — must be at least 32 bytes. Generate a real
# secret for production, e.g.: openssl rand -base64 48
JWT_SECRET=dev-only-secret-change-me-in-production-32bytes-min
JWT_EXPIRATION_MS=3600000
